import FirebaseCore
import FirebaseAuth
import GoogleSignIn
import SwiftUI

struct ContentView: View {
    @State var text = ""
    var body: some View {
        VStack {
            
            // User login
            // Create a new user if it's new
            // Every user has a list of tasks to do
            
        }
        .padding()
    }
}

#Preview {
    ContentView()
}

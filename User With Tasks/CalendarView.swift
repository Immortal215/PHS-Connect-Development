import SwiftUI

struct CalendarView: View {
    var viewModel : AuthenticationViewModel

    var body: some View {
        VStack {
            Text("Calendar")
                .font(.title)
            
        }
    }
}

